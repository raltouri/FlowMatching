# Raw images: 3.5 GB, downloaded automatically by torchvision.
# Only whoever runs extraction needs them.
data/

# features/ is committed on purpose (~48 MB) so the group shares the caches
# via git pull and nobody else has to download the datasets.

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Working plans — kept local, not published
PLAN_STAGE*.md

# macOS
.DS_Store
